import { Map } from "lucide-react"

import PagePlaceholder from "#/components/shomaui/page-placeholder"

const RoadmapPage = () => (
  <PagePlaceholder
    title="Roadmap"
    description="See every SHOMAUI release and what is shipping next."
    icon={Map}
  />
)

export default RoadmapPage
